BroadWatch Frontend (Next.js)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:3000

This is a minimal scaffold with Home and Event pages. I'll connect it to the Flask API next.
